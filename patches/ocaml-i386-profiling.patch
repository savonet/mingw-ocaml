Make sure that PROFILE_* macros are defined on MinGW.  Not sure
what to put in them yet, so at the moment they are just empty.

Index: mingw-ocaml/ocaml/asmrun/i386.S
===================================================================
--- mingw-ocaml.orig/ocaml/asmrun/i386.S	2013-03-21 19:42:04.566032628 -0500
+++ mingw-ocaml/ocaml/asmrun/i386.S	2013-03-21 19:42:04.546042628 -0500
@@ -92,6 +92,9 @@
         movl %esp, %ebp; \
         call .mcount; \
         popl %ebp; CFI_ADJUST(-4)
+#elif defined(SYS_mingw)
+#define PROFILE_CAML
+#define PROFILE_C
 #elif defined(SYS_macosx)
 #define PROFILE_CAML \
         pushl %ebp; CFI_ADJUST(4); \
